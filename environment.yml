name: unicore-eeg
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch
      - torchvision
      - torchaudio
      - numpy
      - scipy
      - matplotlib
      - tqdm
      - mne>=1.8,<2
      - wfdb>=4.1,<5
      - openneuro-py>=2024.2
      - h5py>=3.11,<4
      - pandas>=2.2,<3
      - scikit-learn>=1.5,<2
      - requests>=2.32,<3
      - tabulate>=0.9,<1
      - pyyaml>=6.0,<7
      - psutil>=5.9,<8
      - pytest>=8,<10
