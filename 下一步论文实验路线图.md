# UniCORE-EEG 下一步论文实验路线图

> 制定日期：2026-09-22  
> 适用代码版本：`b746c43`（同一套权重支持不同通道数）  
> 本文用途：指导后续实验实施、结果验收和论文写作。原执行手册仍负责工程任务定义；本文按论文证据链重新组织优先级。

---

## 1. 当前研究状态

### 1.1 已完成的工作

目前已经具备以下基础：

1. 六专家 EEG 去伪迹模型已经可以训练和推理，包括 Harmonic、Ocular/Drift、Myogenic、Cardiac、Motion/Transient 和 Unknown。
2. 已实现自主多标签路由、自适应 Top-r、clean-bypass、专家专属输出头、残差细化和身份门。
3. 已完成 montage 坐标表、真实空间混音合成器和空间投影模块。
4. 已把模型改造成通道共享结构：同一个模型实例和同一份权重可以接受不同的通道数，参数量不随通道数变化。
5. 已支持同一 batch 内混合不同通道数，通过 `channel_mask` 屏蔽补齐通道。
6. 已验证通道重排等变、padding 屏蔽、混合 C 反向传播和 C=1/2/3/34/64/128 的前向能力。
7. 当前测试为 100 个测试通过、105 个子测试通过。
8. 阶段 0 的环境、历史基线、只读数据池探测和可复现性基础已经完成。

### 1.2 目前还不能得出的结论

当前结果证明的是“结构上可以跨通道数使用”，还不能直接形成论文中的性能结论。原因如下：

- `runs/smoke_multichannel/summary.json` 是旧架构生成的，其中参数量仍随 C 增长，不能作为当前共享权重架构的最终证据。
- `runs/_regress/regress_multichannel.md` 中的单通道回归发生在本次整模型重构之前，必须用新架构重新训练和复核。
- 当前没有一套经过多通道联合训练的新 checkpoint。
- 还没有比较“一个跨 C 通用模型”和“每个 C 单独训练的专用模型”。
- 还没有系统测试未见通道数、未见 montage、随机缺导和真实数据迁移。
- ds004784 的物理真值尚未完成 trigger 对齐和监督对构造。
- 当前已知存在 cardiac 被路由到 ocular/drift、真实域 clean-bypass 失效等风险，旧结果不能掩盖这些问题。

因此，下一步应先完成新的 Gate 1，再进入数据基础设施和正式训练。不要直接使用旧 checkpoint 做论文主实验。

---

## 2. 论文应回答的核心问题

建议把论文的主问题限定为以下四个，避免实验范围失控。

### RQ1：同一套权重能否处理不同通道数？

同一 checkpoint 是否能直接处理不同 C，并在已见和未见通道数下保持稳定，不需要修改网络层或重新训练。

### RQ2：跨 C 通用模型是否接近专用模型？

相对于“每种通道数分别训练一个模型”，通用模型是否以一份参数和一份 checkpoint 达到非劣效性能，同时显著降低模型存储、部署和维护成本。

### RQ3：泛化来源是什么？

性能来自共享时序权重、坐标条件化、真空间混音、通道掩码，还是仅来自训练数据规模。需要用消融实验区分。

### RQ4：模型是否在真实数据上有效且不误删生理信号？

需要在物理真值、真实伪迹标签、清洁输入、下游任务和生理负对照上分别回答。合成数据上的高指标不能替代真实验证。

---

## 3. 预注册假设与主要终点

正式训练前，应把下面内容写入 `configs/hypotheses.yaml` 或独立的预注册文件，并提交到 Git。所有阈值必须在看到正式测试结果之前固定。

### 3.1 建议假设

| 编号 | 假设 | 主要证据 |
|---|---|---|
| H1 | 一份 checkpoint 可在 C=1–128 间直接运行，参数量和权重键完全不变 | 结构检查、同 checkpoint 前向与反向 |
| H2 | 通用模型在已见 C 上相对专用模型非劣效 | 按 C 配对的 RRMSE、SNR 改善和 macro-F1 |
| H3 | 通用模型能泛化到训练时未见的通道数 | leave-one-C-out 实验 |
| H4 | 通用模型能泛化到未见 montage，而不仅是未见 C | leave-one-montage-out 实验 |
| H5 | 坐标条件化主要改善 ocular 和 cardiac 等具有空间结构的伪迹 | 空间模块消融和类别分层结果 |
| H6 | 随机缺导时性能平滑下降，不发生突然失效 | channel dropout 曲线 |
| H7 | 去伪迹不会显著破坏清洁 EEG、病理波形或下游任务信息 | clean-input、负对照和 BCI2a 下游实验 |

### 3.2 主要指标

不要只用一个总分。建议把终点分为四组：

1. 信号恢复：RRMSE、时域相关、频域相关、SNR improvement、伪迹残余比。
2. 路由质量：每类 AUROC、macro-F1、ECE、Unknown false alarm、严重度相关性。
3. 生理保持：Delta–Gamma 频带功率误差、Alpha 保持、clean modification MAE、clean-bypass rate。
4. 跨通道能力：按 C 和 montage 分层的性能、失败率、显存、推理时间、参数量与 checkpoint 存储量。

主要结论应优先基于 RRMSE、SNR improvement、macro-F1 和 clean modification MAE。其余作为支持指标。

### 3.3 非劣效边界

通用模型与专用模型比较时，应采用非劣效设计，而不是要求通用模型在每项指标上显著更好。建议在正式结果产生前固定以下初始边界：

- macro-F1 或 AUROC：通用模型不低于专用模型 0.02 以上。
- RRMSE：通用模型不高于专用模型 5% 以上。
- SNR improvement：通用模型不低于专用模型 0.5 dB 以上。
- clean modification MAE：通用模型不高于专用模型 10% 以上。

这些边界必须结合领域意义说明。如果后续认为边界需要修改，应先记录理由，再产生新的正式结果；不能根据结果反向修改。

---

## 4. 第一优先级：重新完成 Gate 1

这是当前立即要做的阶段。预计产物放在 `runs/gate1_shared_weights/`。

### 4.1 重新运行新架构 smoke

使用同一个已实例化模型依次处理 C ∈ {1, 3, 8, 34, 64, 128}，并对每个 C 完成前向、损失、反向一步。

必须记录：

- 所有 C 的参数量必须完全相同。
- 输出 clean 和六个分量的形状。
- loss 是否 finite。
- 梯度是否 finite。
- 每个 C 的显存峰值与单步耗时。
- 同一份 `state_dict` 严格加载后是否仍可处理所有 C。
- 失败时完整记录异常，不能跳过失败的 C 后写“整体通过”。

验收条件：六种 C 全部成功，参数量一致，输出和梯度无 NaN/Inf。

### 4.2 完成 C=8 短训练

用标准 8 导子集 `Fp1 Fp2 F7 F8 O1 O2 C3 C4` 运行 2048 个训练样本、2 个 epoch。

必须保存：

- 配置快照和 Git commit。
- 每轮训练/验证损失。
- checkpoint。
- 参数 finite 检查。
- 训练前后同一批样本的指标变化。

这一步只用于验证训练闭环，不作为论文性能结果。

### 4.3 新架构单通道回归

按照旧单通道基线的相同数据、相同种子、相同训练规模，重新训练 C=1 的新架构。比较对象至少包括：

1. 历史单通道基线。
2. 真混音改造后的旧架构结果。
3. 当前通道共享新架构结果。

若 macro-AUROC 相对旧基线差超过 0.03，先检查优化和容量变化，不进入阶段 2。即使通过，也要同时报告 RRMSE、SNR improvement、clean-bypass 和各类别混淆，不能只看 macro-AUROC。

### 4.4 混合 C 最小训练验证

构造至少包含 C={1, 3, 8, 34} 的同一训练 batch，随机打乱样本顺序。检查：

- padding 通道输出严格为零或被损失完全屏蔽。
- 有效通道结果不受同 batch 中最大 C 改变影响。
- 单独计算样本和混合 batch 计算样本的输出在数值容差内一致。
- 反向后所有应训练参数都有 finite 梯度。

### 4.5 Gate 1 放行条件

只有以下项目全部满足，才进入阶段 2：

- 新架构六种 C smoke 全部通过。
- 同一模型、同一 checkpoint、同一参数量得到明确记录。
- C=8 短训练完成且 checkpoint finite。
- 新架构 C=1 回归通过。
- 混合 C batch、通道置换、padding 和缺导测试通过。
- 全部自动测试通过。
- `runs/stage1/NOTES.md` 更新为新架构结果，旧结果明确标成“重构前”。

---

## 5. 第二优先级：建立无泄漏的数据基础设施

Gate 1 完成后，先做好数据再进行长时间训练。论文中最难补救的问题通常是数据泄漏，而不是模型代码。

### 5.1 统一样本结构和预处理

实现统一记录字段和批处理接口。预处理顺序固定为：

`CAR → 抗混叠重采样 → 0.5–45 Hz 带通 → robust MAD 裁剪`

每条样本至少保存：数据集、受试者、记录、会话、时间范围、采样率、通道名、坐标、标签有效性、分量真值有效性和增强记录。

注意：PhysioMotion 原始数据是 1000 Hz，必须使用抗混叠重采样，不能用普通线性插值降到 500 Hz。

### 5.2 先划分记录，再切窗口

所有数据集遵循：

1. 先按 subject_id 划分 train/validation/test。
2. 没有 subject_id 时按 recording_id 划分。
3. 划分完成后才允许切窗。
4. 同一原始片段产生的不同视图、增强版本和重叠窗口只能属于一个集合。

严禁随机打散窗口后再划分。这会显著抬高论文结果。

### 5.3 泄漏检查

训练脚本启动前强制检查四类泄漏：受试者跨集合、记录跨集合、窗口时间重叠、清洁源或伪迹源跨集合复用。任何一项非零均停止训练。

每次正式实验保存 `splits.json` 和 `leakage_report.json`。后续所有表格应能追溯到具体划分文件。

### 5.4 ds004784 物理真值

这是近期最重要的真实数据任务。建议独立完成，不与训练代码同时改动。

执行顺序：

1. 用 `x65471` 事件完成 GT 和 BIDS 的首尾锚点线性对齐。
2. 先只在 Brain 条件验证，脑源投影与 BIDS 的相关必须大于 0.5。
3. 估计 GT 前 20 路到 128 导的混合矩阵并保存拟合残差。
4. 构造 clean、ocular、myogenic 三类可信分量真值。
5. harmonic、cardiac 和 unknown 的 `component_mask` 必须为 0，禁止伪造监督。
6. 只有一个体模受试者，因此按 recording/task 做留一任务交叉验证，论文中明确结论只适用于该体模。

若 Brain 条件相关不达 0.5，应停止并输出失败报告，不能通过手工偏移挑选最好结果。

### 5.5 外部真实数据

数据集的角色应预先固定：

| 数据集 | 主要用途 | 不能声称的内容 |
|---|---|---|
| ds004784 | 物理真值、分量恢复、清洁负对照、复合伪迹 | 不能代表多受试者临床泛化 |
| PhysioMotion | 真实 ocular/myogenic/motion 路由和跨数据集验证 | 无逐点 clean 真值时不能报告精确重建误差 |
| BCI2a | 下游运动想象、清洁保持、多通道迁移 | 不能把分类变化直接解释为去伪迹真值 |
| CHB-MIT | 病理生理负对照 | 发作和棘波不能作为应删除伪迹 |
| Sleep-EDF/CAP | 纺锤波、K 复合波保持 | 事件标签未核验前不能用于定量结论 |

---

## 6. 第三优先级：训练真正的跨通道通用 checkpoint

### 6.1 训练集的通道数设计

训练时不要让大 C 数据支配梯度。建议按样本而不是按总通道点数平衡采样，并使每个 epoch 中各 C 桶出现次数接近。

建议训练 C 集合：`{1, 3, 8, 19, 22, 34, 64, 128}`。如果某个 C 没有可靠 montage，则用标准参考表的固定子集，并保存具体通道名。

每个 batch 可以是：

- 同 C batch：吞吐更高，适合主要训练。
- 混合 C batch：占 20%–30%，用于训练 mask 稳定性。

不要只用连续编号的前 C 个电极。应从真实 montage 或预注册的空间覆盖子集中选通道，避免通道子集偏向头皮某一区域。

### 6.2 通道缺失增强

对多通道样本随机丢弃 0%–40% 通道，至少保留 1 个有效通道。丢弃后同步更新信号、坐标和 `channel_mask`。

建议缺导比例分布：

- 50% 样本不丢通道。
- 20% 丢 1–10%。
- 20% 丢 10–25%。
- 10% 丢 25–40%。

不要在正式测试时使用与训练完全相同的缺导随机种子。

### 6.3 分阶段训练

按照以下顺序进行：

1. 阶段 A：令牌和专家预训练。包含清洁、单伪迹、留一类异常和有分量真值样本。
2. 阶段 B：加入复合伪迹和已知+未知组合，训练统一粗分解和自适应路由。
3. 阶段 C：冻结粗分解部分，只训练残差细化器。
4. 阶段 D：以阶段 C 学习率的 0.1–0.2 倍联合微调。

训练 batch 中必须同时保留清洁、单伪迹、复合伪迹和 Unknown 样本。Unknown 不能只是所有未解释残差的默认出口。

### 6.4 模型选择

checkpoint 不能只按 MSE 选择。建议预注册一个综合验证规则，至少包含：

- RRMSE 越低越好。
- 频带保持越高越好。
- macro-F1 越高越好。
- clean modification 越低越好。
- Unknown clean false alarm 越低越好。

各指标应先根据验证集的固定尺度标准化，再加权，防止单位较大的指标支配总分。权重必须在第一次正式训练之前固定。

### 6.5 随机种子

开发期可用 seed 42 做流水线调试。正式实验必须完整运行 42、1234、20260922 三个种子，且每个种子都从初始化开始训练，不能只在最终评估时改变随机种子。

---

## 7. 跨通道能力的核心论文实验

这是论文最有区分度的部分，应单独设计成主表和主图。

### 7.1 实验 A：已见 C 的统一模型与专用模型

对每个 C ∈ {1, 3, 8, 19/22, 34, 64, 128} 比较：

- Universal：一套权重联合训练所有 C。
- Specialist-C：仅使用该 C 训练的独立模型。
- Universal-no-coord：通用模型关闭坐标条件化。
- Universal-no-dropout：不做随机缺导增强。

训练样本预算要公平。建议同时报告两种口径：

1. 每个模型看到相同总样本数。
2. 每种 C 看到相同样本数。

主张应基于 Universal 与 Specialist-C 的非劣效比较。参数量和磁盘占用按部署所需模型总数计算：专用方案是所有专用 checkpoint 之和，通用方案是一份。

### 7.2 实验 B：leave-one-C-out

每次从训练中完全移除一个 C，仅在测试时使用。例如：

- 训练 {1,3,8,34,64,128}，测试 C=22。
- 训练 {1,3,8,22,64,128}，测试 C=34。
- 训练 {1,3,8,22,34,128}，测试 C=64。

至少选择低、中、高三个未见 C。不能只把已见 montage 随机删几根导联当作唯一的“未见通道数”。

### 7.3 实验 C：leave-one-montage-out

训练时完全移除一个 montage，测试时使用其原始通道名、坐标和通道数。若训练集里存在相同 C 的其它 montage，便可以区分“通道数泛化”和“空间布局泛化”。

建议至少测试：

- BCI2a 22 导。
- PhysioMotion 34 双极导。
- ds004784 128 导体模。

双极导联坐标是两端中点近似，必须单列结果，不应与单极 montage 静默合并。

### 7.4 实验 D：缺导鲁棒性曲线

对每个真实 montage 固定一批测试记录，随机删除 0%、10%、20%、30%、40%、50% 通道。每个比例至少重复 20 个确定性随机掩码。

画出性能随有效通道比例变化的曲线，并报告：

- 均值和 95% 置信区间。
- 最差 5% 掩码性能。
- 前额、中央、枕区成组缺失的结果。
- 随机缺失与空间连续区域缺失的差异。

### 7.5 实验 E：通道重排与批次不变性

这是性质验证，不需要统计显著性：

- 输入、坐标、mask 同步重排后，逐通道输出应按同样顺序重排。
- 同一样本单独推理和放入不同最大 C 的 padded batch 推理，输出应在预设数值容差内一致。
- 重复通道或错误坐标不属于模型承诺范围，应作为失败案例报告，不要混入正常测试。

---

## 8. 消融实验

消融必须每次只改变一个因素，并保持数据划分、训练步数、优化器和种子一致。

建议最低限度包含：

| 消融 | 要回答的问题 |
|---|---|
| 去掉坐标条件化 | 跨 montage 能力是否依赖坐标 |
| 去掉 SpatialProjectionHead | 空间投影是否改善 ocular/cardiac |
| 真空间混音改回复制式多通道 | 性能是否真的来自空间训练信号 |
| 去掉 channel dropout | 缺导鲁棒性是否来自增强 |
| 去掉混合 C batch | batch 内 mask 训练是否必要 |
| 共享权重改为每 C 专用模型 | 通用性带来的性能代价 |
| 固定 Top-2 / 固定 Top-4 / 自适应 Top-r | 路由策略是否有效 |
| 去掉 Unknown 专家 | 开放集性能和已知类污染如何变化 |
| 去掉 clean-bypass | 清洁输入的误修改程度 |
| 去掉残差细化器 | 粗分解与细化各自贡献 |

如果计算预算有限，优先保留前六项，它们直接支撑“同一套权重跨通道数”的论文主张。

---

## 9. 正式评价和统计方法

### 9.1 实验单位

统计单位必须是受试者或独立记录，不能把高度相关的窗口当作独立样本计算 p 值。窗口指标先在记录内汇总，再进行受试者级或记录级统计。

ds004784 只有一个体模，应使用任务/记录级结果和留一任务交叉验证，不做虚假的受试者级显著性检验。

### 9.2 置信区间

建议对受试者进行 bootstrap，至少 2000 次重采样，报告 95% 置信区间。对于只有少量独立记录的数据集，同时给出所有记录的散点，不只给均值柱状图。

### 9.3 配对比较

同一受试者、同一记录上的模型比较使用配对设计：

- 分布近似对称时使用配对 t 检验。
- 偏态或样本较少时使用 Wilcoxon signed-rank。
- 同时报告效应量和置信区间，不能只报告 p 值。
- 多模型、多指标比较使用 Holm 校正。

非劣效检验应使用配对差值的单侧 95% 置信区间，并与预注册边界比较。

### 9.4 三种子汇总

每个表格同时给出：

- 每个随机种子的结果。
- 三种子 mean ± standard deviation。
- 受试者或记录级分布。
- 最好、典型和最差案例。

不能只挑最好 seed，也不能把三个 seed 的所有窗口混在一起当作扩大样本量。

---

## 10. 外部基线与公平比较

在运行基线之前建立 `configs/baselines.yaml` 并提交。至少包括：

1. 传统多通道方法：ICA + 自动成分判别、ASR。
2. 参数量接近的共享编码器模型。
3. 多个伪迹专用模型串联。
4. 无条件统一模型。
5. 使用真实标签路由的 oracle 上界。
6. 不做任何处理的 identity 基线。

公平比较要求：

- 相同 train/validation/test 划分。
- 相同采样率和评估窗口。
- 相同可用通道和信息。
- 超参数只能在验证集选择。
- 单通道下不能运行的 ICA/ASR 标注“不适用”，不计为性能失败。
- 外部模型若输入或训练数据无法对齐，则只做定性参考，不进入主排名。

---

## 11. 真实数据、清洁输入和负对照

### 11.1 PhysioMotion

主要评价多标签路由、跨数据集泛化和真实运动伪迹。由于没有逐点 clean 真值，重点报告事件级检测、伪迹时段前后变化、频谱变化及人工检查，不报告虚假的 clean RRMSE。

### 11.2 Clean-input

使用 ds004784 Brain、BCI2a 可确认的清洁/静息片段和合成 clean。报告：

- bypass rate。
- input modification MAE。
- 各频带功率变化。
- 路由误激活率。
- 最差修改案例。

### 11.3 生理负对照

CHB-MIT 的棘波/发作、Sleep-EDF/CAP 的纺锤波和 K 复合波应被保留。先核验事件标注质量，再报告处理前后幅值、形态、频带能量和事件检测保持率。

### 11.4 下游任务

优先使用 BCI2a 运动想象。按受试者内交叉验证比较原始信号、通用模型处理后和基线处理后。报告逐受试者准确率、置信区间、配对检验、效应量和劣化受试者。

去伪迹后分类提高只能说明下游可用性改善，不能反向证明每个预测分量都是真实伪迹。

---

## 12. 失败判据和回退原则

以下情况应停止当前阶段并定位原因：

1. 新架构 C=1 macro-AUROC 相对基线下降超过 0.03。
2. 同一 checkpoint 在任一 C 上出现 NaN、Inf 或形状错误。
3. padded batch 改变有效通道输出，且误差超过数值容差。
4. ds004784 Brain 对齐相关低于 0.5。
5. 任一数据划分泄漏计数不为 0。
6. cardiac→ocular/drift 路由比例仍超过 0.5，应优先检查观测特征和监督，而不是继续扩大训练。
7. clean-bypass 在真实清洁输入上接近 0，应先处理校准和域偏移。
8. 通用模型在多个 C 上越过非劣效边界，应分别检查容量、采样平衡和坐标编码，不应只通过增加 epoch 掩盖问题。

所有失败都应保留配置、日志和失败报告。失败实验是论文方法边界的一部分，不应删除记录。

---

## 13. 推荐执行顺序

### 第 1 周：Gate 1 收尾

1. 重新运行当前新架构六种 C smoke。
2. 完成混合 C batch 一致性验证。
3. 运行 C=8 两 epoch 短训练。
4. 重新训练新架构 C=1 全量回归。
5. 更新 Gate 1 报告并冻结阶段 1 代码。

### 第 2–3 周：数据基础设施

1. 统一预处理和样本记录。
2. 实现受试者/记录级划分和泄漏检查。
3. 完成 ds004784 对齐与物理真值对。
4. 完成 PhysioMotion 标签映射和抗混叠重采样。
5. 让 BCI2a、CHB-MIT、Sleep-EDF loader 通过 dry-run。

### 第 4 周：小规模方法筛选

1. 用 seed 42、缩小样本量跑完整 A–D 流程。
2. 检查 cardiac/ocular 混淆、Unknown 和 clean-bypass。
3. 固定选择指标、非劣效界限、基线和正式配置。
4. 只修复方法缺陷，不根据测试集调整超参数。

### 第 5–7 周：正式三种子训练

1. 训练一套 Universal checkpoint，三种子。
2. 训练必要的 Specialist-C 对照。
3. 运行核心消融。
4. 所有训练仅使用 train/validation，正式 test 保持封存。

### 第 8–9 周：正式评价

1. 已见 C、leave-one-C-out、leave-one-montage-out。
2. 缺导鲁棒性曲线。
3. ds004784、PhysioMotion 和 clean-input。
4. 外部基线和 BCI2a 下游。
5. 生理负对照和失败案例。

### 第 10 周：统计与论文产物

1. 受试者/记录级统计和多重比较校正。
2. 生成主表、消融表、泛化曲线和 Pareto 图。
3. 汇总模型大小、推理时间和显存。
4. 写清结论边界，特别区分合成、体模和真实受试者证据。

---

## 14. 论文建议的主表和主图

### 主表

- 表 1：数据集、受试者、记录、采样率、C、montage、标签与真值类型。
- 表 2：Universal 与 Specialist-C 在各 C 上的主要指标和非劣效结论。
- 表 3：跨数据集、clean-input、负对照和下游结果。
- 表 4：关键消融和三种子统计。

### 主图

- 图 1：共享权重、坐标条件化和 channel mask 的模型结构。
- 图 2：性能随通道数变化，已见 C 与未见 C 分开画。
- 图 3：缺导比例与性能的鲁棒性曲线。
- 图 4：去除率与生理失真的 Pareto 前沿。
- 图 5：各伪迹类别的路由混淆、专家激活和代表性时频案例。

图中误差条必须说明来自随机种子、受试者还是记录，不能混用。

---

## 15. 最终完成标准

项目达到可投稿状态至少需要：

- 一套真正训练完成的跨 C 通用 checkpoint。
- 三个完整随机种子。
- Universal 对 Specialist-C 的公平非劣效比较。
- 已见 C、未见 C、未见 montage 和缺导实验。
- 无数据泄漏的受试者/记录级划分证据。
- 至少一个物理真值数据集和一个真实多受试者外部数据集。
- clean-input 与生理负对照。
- 至少一个下游任务。
- 外部基线和关键消融。
- 受试者/记录级置信区间、效应量和多重比较校正。
- 明确报告失败案例和适用边界。

当前最具体的下一步是：**先重新运行新架构的 Gate 1 smoke，再做 C=8 短训练和新架构 C=1 回归。三项全部通过后，开始 ds004784 对齐和无泄漏数据管线。**
